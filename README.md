# scan-businesscard-for-notion

名刺画像をOpenAI APIを利用して解析し、Notionに登録する

## Current status

The Laravel project scaffold could not be initialized because outbound connections to Packagist
are blocked in this environment (HTTP 403 during `composer create-project`). The repository
remains without the framework code until network access is available.
